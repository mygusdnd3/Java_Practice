package day02;

public class Test08 {

	/*
	 * 참고] 이클립스에서 블력을 만들경우 {} 사용하게 되는데 이때 이클립스는 자동으로 닫는 괄호(}) 붙여준다
	 * 
	 */

	public static void main(String[] args) {
		float num = 10; // 자동 형변환
		// float num2 = 10.;//오버플로우
		float num2;
		num2 = 10.F; // 리터럴 형변환
		num2 = (float) 10.; // 강제

		/*
		 * & - 그리고and
		 * | - 또는 or
		 * && - 절삭 연산을 한다 a&&b a가 false면 b는 보지않는다.
		 * || - 절삭 연산을 한다 둘중에 하나만 참이면 참이다.
		 */
	}
}
